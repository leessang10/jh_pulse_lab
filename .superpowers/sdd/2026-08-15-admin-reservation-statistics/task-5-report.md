# Task 5 보고서

## 구현

- URL 검색 파라미터를 기준 월·추세 단위·지표의 단일 조회 상태로 정규화했습니다.
- 상단 컨트롤을 `기준 월 → 일/주/년 → 정기권 시뮬레이션` 순서로 만들고, 미래 월은 선택지에서 제외했습니다.
- KPI 5개와 로딩 Skeleton, 오류 Alert·재시도, 빈 상태를 추가했습니다.
- `buildStatisticsSummaryView`로 이용시간, 평균, 취소율과 이전 월 비교 문구를 계산했습니다.

## 검증

- `npm test -- src/lib/admin-statistics.test.ts` 통과 (10개)
- `npm test` 통과 (29개 파일, 133개)
- `npx tsc --noEmit` 통과
- `npm run build` 통과

## 자기검토

- 취소 예약은 이용시간·이용자·평균에서 제외되고, 총 예약 건수·취소율에는 포함됩니다.
- 비교 불가 문구와 모든 상태 문구는 존댓말입니다.
- 시뮬레이터는 다음 Task에서 Dialog를 연결할 수 있도록 open 상태만 준비했습니다.
